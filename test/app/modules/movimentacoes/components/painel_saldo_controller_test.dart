import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dart_week_mobile/app/modules/movimentacoes/components/painel_saldo_controller.dart';
import 'package:dart_week_mobile/app/modules/movimentacoes/movimentacoes_module.dart';

void main() {
  initModule(MovimentacoesModule());
  // PainelSaldoController painelsaldo;
  //
  setUp(() {
    //     painelsaldo = MovimentacoesModule.to.get<PainelSaldoController>();
  });

  group('PainelSaldoController Test', () {
    //   test("First Test", () {
    //     expect(painelsaldo, isInstanceOf<PainelSaldoController>());
    //   });

    //   test("Set Value", () {
    //     expect(painelsaldo.value, equals(0));
    //     painelsaldo.increment();
    //     expect(painelsaldo.value, equals(1));
    //   });
  });
}
